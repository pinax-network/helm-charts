{{- $values := $.Values.inject }}
{{- $componentName := "inject" }}
{{- $componentLabel := include "blockmeta.componentLabelFor" $componentName }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "inject.uniqueName" . }}-svc
  labels:
    app.kubernetes.io/name: {{ include "inject.uniqueName" . }}-svc
spec:
  {{- with $values.service.type }}
  type: {{ . }}
  {{- else }}
  type: ClusterIP
  {{- end }}
  {{- with $values.service.ipFamilyPolicy }}
  ipFamilyPolicy: {{ . }}
  {{- end }}
  {{- with $values.service.clusterIP }}
  clusterIP: {{ . }}
  {{- end }}
  ports:
  {{- range $portName, $portNumber := $values.service.ports }}
    - port: {{ $portNumber }} # the port we expose on the Service, user configurable
      targetPort: {{ $portName }} # the name of the port on the container that we are routing to
      protocol: TCP
      name: {{ $portName }}
  {{- end }}
  selector:
    app.kubernetes.io/name: {{ include "inject.uniqueName" . }}

---
{{- if .Values.inject.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "inject.uniqueName" . }}-monitor
spec:
  endpoints:
    - honorLabels: true
      interval: {{ .Values.inject.serviceMonitor.interval | default "30s" }}
      scrapeTimeout: {{ .Values.inject.serviceMonitor.scrapeTimeout | default "10s" }}
      port: metrics
      path: /metrics
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "inject.uniqueName" . }}-svc
{{- end }}