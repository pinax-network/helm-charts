apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}- {{- .Values.chain }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}
    chain: {{ .Values.chain }}
spec:
  selector:
    matchLabels:
      app: {{ .Release.Name }}
      chain: {{ .Values.chain }}
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
        chain: {{ .Values.chain }}
    spec:
      containers:
      - name: {{ .Release.Name }}- {{- .Values.chain }}
        image: {{ .Values.image -}} : {{- .Values.tag }}
        args: ["run"]
        ports:
        - containerPort: {{ .Values.env.port }}
        envFrom:
        - configMapRef:
            name: env
        - secretRef:
            name: {{ .Values.secretName }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.env.port }}
          initialDelaySeconds: 20
          timeoutSeconds: 2
          periodSeconds: 5
        {{- if .Values.cursorPvc.enabled }}
        volumeMounts:
            - name: cursor-volume
              mountPath: /cursor
      volumes:
        - name: cursor-volume
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}- {{- .Values.chain }}
      {{- end }}