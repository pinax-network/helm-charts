CREATE TABLE IF NOT EXISTS eos_tokens_v1.backprocess_test
(
    id        String,
    cursor    String,
    block_num Int64,
    block_id  String
)
    ENGINE = ReplacingMergeTree()
        PRIMARY KEY (id)
        ORDER BY (id);